import React from "react";
import { Container, Button } from "./style";
const Hero = () => {
  return (
    <Container>
      <Button>CONTACT US</Button>
    </Container>
  );
};

export default Hero;
